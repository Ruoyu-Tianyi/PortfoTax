# PortfoTax · 投后财税雷达 —— 使用说明

> 面向首次使用者的操作指南 · 数据为虚构演示数据 · 原型版本（Demo 级）

---

## 一、产品简介

PortfoTax 投后财税雷达是一个面向 PE/VC 投后管理场景的轻量级纯前端原型：它把被投企业的三张财务报表关键科目、纳税申报记录和开票数据放在一起，自动执行 6 条财税一致性校验规则（R1–R6），为每家企业计算 0–100 分的「财税健康分」并给出绿 / 黄 / 红三档风险等级；对命中的规则生成「AI 风险解读」与处置建议（原型中为模板化模拟输出）；同时用申报日历监测各企业各税种的申报状态，逾期事项自动按万分之五/日估算滞纳金，最终可一键生成单企业的《月度财税体检报告》并经浏览器打印导出为 PDF。

---

## 二、运行方式

本原型为纯前端单页应用，**无需安装、无需联网、无后端、无数据库**。

### 方式 A：直接双击打开（最简单）

1. 进入 `app` 文件夹；
2. 双击 `index.html`；
3. 系统默认浏览器会打开页面，自动进入「组合总览」。

全部脚本通过普通 `<script>` 标签加载，兼容 `file://` 协议，双击即可运行。

### 方式 B：起本地服务（可选，适合演示投屏）

1. 打开命令行，进入 `app` 目录：
   ```bash
   cd app
   python -m http.server 8080
   ```
2. 在浏览器地址栏访问 `http://localhost:8080`。

### 浏览器要求

- 推荐使用 **Chrome 或 Microsoft Edge** 的最新版本；
- 建议使用桌面端浏览器（窗口宽度 ≥ 1280px 显示效果最佳）。

### 页面导航

页面顶部导航栏有两个入口：「组合总览」和「申报监测」。「企业详情页」和「体检报告页」不直接出现在导航栏中，需从卡片或链接点入。所有页面均为同一窗口内切换，可随时点导航栏返回。

---

## 三、四个页面操作说明

### 3.1 组合总览（Dashboard）

打开应用后默认进入本页。页面副标题栏显示当前基金名称「智汇创投二期基金」、数据期间「2026 年 5 月」与数据基准日「2026-06-20」。

**① 组合级 KPI（顶部 4 张指标卡）**

| 指标 | 怎么看 |
|---|---|
| 「组合平均健康分」 | 6 家企业健康分的平均值（演示数据为 67 分），数字颜色随分值区间变化（绿 ≥ 90 / 黄 60–89 / 红 < 60）；下方小字注明覆盖企业数与基准日。 |
| 「红色预警企业」 | 红色等级企业数量（演示数据为 2 / 6 家）；下方小字提示黄色关注企业数。 |
| 「本月待申报事项」 | 全部企业中状态为「临期」或「逾期」的申报事项总数（演示数据为 7 项）；下方小字注明其中逾期几项、滞纳金估算合计多少元。 |
| 「规则命中次数」 | 全部企业 R1–R6 校验命中次数之和（演示数据为 5 次）。 |

**② 被投企业卡片墙**

- 每家企业一张卡片，依次展示：企业名称、行业 · 轮次 · 地区、风险等级徽章（绿 / 黄 / 红）、健康分数字与进度条、命中规则标签（如「R2 账税一致性」；全部通过则显示「6 项规则全部通过」）、最近申报状态（如「2 项申报逾期」红色提示 / 「有临期申报事项」琥珀色提示 / 「最近申报：增值税（2026-06-12）」）。
- **点击任意卡片即进入该企业的「企业详情页」**。

**③ 企业健康分排行（左下）**

- 按健康分从高到低排列，每行显示名次、企业名、等级徽章和分数。
- 点击企业名同样可跳转「企业详情页」。

**④ 风险分布（右下）**

- 条形图按风险类型统计命中数量：R1–R6 各为「命中该规则的企业数」，另附「申报逾期（项）」和「申报临期（项）」两个汇总条。
- 条形越长按比例越长，无命中的类型显示为浅色短条。

页面右上角有「查看申报监测 →」按钮，可直达申报日历页。

### 3.2 企业详情页

从 Dashboard 点击卡片或排行榜进入。页面顶部显示企业名、等级徽章（如「红色 · 16 分」），副标题为行业 · 轮次 · 地区 · 投资时点 · 数据期间。

**① 三表及申报关键数据（左上表格）**

- 单位均为万元，每行一个科目：金额 + 口径来源（利润表 / 资产负债表 / 现金流量表 / 纳税申报 / 税控发票 / 薪酬账）。
- 共 11 个科目：营业收入（账面）、增值税申报销售额、开票金额、利润总额、企税应纳税所得额、净利润、未分配利润（期初 → 期末）、本期分红、销售商品收到现金、工资总额、社保/个税申报基数。
- **用法**：对照阅读「同一业务事实的两个口径」（如「营业收入（账面）」与「增值税申报销售额」），它们的差异正是下方规则校验的输入。

**② 本期申报状态（右上表格）**

- 每行一个税种：所属期、截止日、申报日、状态徽章（「已申报」绿 / 「临期」琥珀 / 「逾期」红 / 「未到期」灰）、逾期天数、滞纳金估算（元）。
- 表格下方注明「滞纳金按《税收征管法》万分之五/日估算，仅供演示」。

**③ 一致性校验结果（R1–R6，页面下半区）**

- 每条规则一个区块，标题行从左到右：规则编号（如「R1」）、规则名、校验公式、结果徽章——绿色「通过」或红色「命中」+「严重度 高 / 中 / 低」徽章（仅命中时显示严重度）。
- 第二行为数据行：两个口径的对比描述（如「净利润 150 万 vs 未分配利润变动+分红 200 万」）、「差异金额」（万元）、「差异率」（百分比，保留 1 位小数）。
- **读法**：先看徽章判断是否命中，再看差异金额与差异率判断量级，最后看严重度判断对健康的冲击（高 / 中 / 低分别扣 30 / 20 / 10 分）。

**④ AI 风险解读（仅命中规则下方出现）**

- 浅黄色卡片，标题形如「✦ AI 风险解读 · 三表勾稽关系断裂」。
- 内容分两段：第一段是自然语言的成因解读（含企业名、差异金额、差异率，以及常见原因枚举）；第二段是 3 条编号处置建议（如「索取未开票收入台账……」）。
- **注意**：原型中该解读由内置模板拼接生成，属于模拟 AI 输出，不构成税务或投资意见（详见 FAQ）。

**⑤ 跳转到体检报告**

- 点击页面右上角「生成体检报告 →」按钮，即进入该企业的体检报告页。
- 页面顶部「组合总览 / XX公司」面包屑可返回 Dashboard。

### 3.3 申报监测日历页

点顶部导航「申报监测」进入，或由 Dashboard 的「查看申报监测 →」进入。

**① 状态图例（右上角）**

- 四种状态实时计数：绿色「已申报」、琥珀色「临期」、红色「逾期」、灰色「未到期」，圆点颜色与数量一一对应。

**② 风险提醒列表（页面上半区表格）**

- 只列出「逾期」和「临期」两类需要行动的事项，全部 6 家企业 × 各税种展开。
- **排序规则**：逾期事项排在前面、按逾期天数从多到少排；临期事项排在后面、按距截止天数从少到多排。
- 「说明」列：逾期事项显示「已逾期 N 天，税额 X 万，滞纳金估算 Y 元（万分之五/日）」；临期事项显示「距截止 N 天，请及时催报」。
- 点击企业名可跳转该企业详情页。

**③ 2026 年 6 月申报日历网格（页面下半区）**

- 月历形式，仅展示申报截止日落在 2026 年 6 月的事项；截止日不在 6 月的事项（如 7 月 15 日的企税季报）不会出现在网格中。
- 每一天的格子里，事项以彩色小条显示，格式为「企业名 · 税种」，**小条底色即状态色**（绿=已申报、琥珀=临期、红=逾期、灰=未到期），鼠标悬停可见完整提示。
- 「基准日」（演示数据中为 2026-06-20）所在格子高亮描边并标注「今天」字样。

**④ 滞纳金估算口径**

- 公式：**滞纳金 = 税额 × 0.05%（万分之五） × 逾期天数**，对应《税收征管法》第三十二条口径，仅供演示；
- 税额以万元存储，界面显示时换算为元并取整。

### 3.4 企业体检报告页

从企业详情页点「生成体检报告 →」进入（路由为 `#/report/企业id`）。

**报告内容（五个部分）**

1. 头部：企业名、报告期间、出具机构、生成日期、行业 / 轮次，大号健康分与等级徽章，以及「规则命中 N 项 · 申报逾期 N 项 · 滞纳金估算 X 元」汇总行；
2. 「一、总体结论」：按绿 / 黄 / 红等级自动生成结论段（绿色=维持关注；黄色=建议 5 个工作日内与 CFO 核对；红色=建议立即启动投后专项核查）；
3. 「二、关键财务与申报数据」：11 个科目金额表（万元）；
4. 「三、一致性校验结果（R1–R6）」：六行表格，含结果（命中/通过）、差异金额、差异率、严重度，命中行以浅红底高亮；
5. 「四、AI 风险解读与处置建议」：仅列命中规则的解读（无命中时显示「本期无命中规则，无需生成风险解读」）；
6. 「五、申报合规情况」：各税种状态与滞纳金估算表。

**打印 / 导出 PDF**

1. 点击报告页右上角「打印 / 导出 PDF」按钮（此时会唤起浏览器打印对话框）；
2. 在打印对话框中，将打印机选择为「另存为 PDF」；
3. 点击「保存」，选择路径即可得到 PDF 文件；
4. 直接连接打印机打印亦可：页面已做打印样式适配，顶部导航栏、面包屑与按钮在打印时自动隐藏，仅输出报告正文。

返回路径：页面顶部面包屑「组合总览 / 企业名 / 体检报告」，可逐级返回。

---

## 四、演示数据说明（6 家虚构企业）

数据基准日固定为 **2026-06-20**，报表期间为 **2026 年 5 月**，金额单位均为万元，所属基金为虚构的「智汇创投二期基金」。6 家企业按绿 2 / 黄 2 / 红 2 设计，覆盖全部 6 条规则与三种申报异常情形：

| 企业 | 行业 · 轮次 · 地区 | 数据设定 | 命中规则 | 申报异常 | 健康分 / 等级 |
|---|---|---|---|---|---|
| 云启智造 | 工业软件 · B轮 · 上海浦东 | 账面、申报、开票三口径完全一致（均为 1200 万），无差异 | 无 | 无（印花税 6-25 截止，距基准日 5 天，未到期） | 100 · 绿 |
| 禾沐生物 | 创新药 · A轮 · 江苏苏州 | 规则全部通过（开票 305 vs 申报 300，差异率 1.6% 低于 5% 阈值） | 无 | 印花税 6-22 截止未申报 → 临期（扣 4 分） | 96 · 绿 |
| 星链物流 | 智慧物流 · C轮 · 浙江杭州 | 账面收入 5000 vs 增值税申报 4600，差异 400 万、差异率 8% | R2（中，扣 20 分）；注意开票 4620 与申报口径接近，R4 不命中，差异指向未开票收入 | 无 | 80 · 黄 |
| 澄光新能源 | 光伏组件 · B轮 · 安徽合肥 | 销售收现 4400 / 收入 8000，收现比 0.55 低于正常区间 | R5（中，扣 20 分） | 个税逾期 5 天（扣 12 分），滞纳金约 450 元 | 68 · 黄 |
| 砺芯半导体 | 芯片设计 · A轮 · 广东深圳 | 净利润 150 vs 未分配利润变动 200+分红 0，差异 50 万（33%）；工资 500 vs 社保基数 350，差异 150 万（30%） | R1（高，扣 30 分）+ R6（高，扣 30 分） | 增值税、个税各逾期 5 天（共扣 24 分），滞纳金合计约 3,875 元 | 16 · 红 |
| 拾味餐饮 | 连锁餐饮 · 天使轮 · 四川成都 | 开票 700 vs 申报收入 600，差异 100 万（14.3%） | R4（中，扣 20 分） | 增值税、个税、印花税 3 项逾期（共扣 36 分），滞纳金合计约 1,205 元 | 44 · 红 |

**绿 / 黄 / 红含义**

- **绿（≥ 90 分）**：财税数据一致性良好，维持常规月度跟踪即可；
- **黄（60–89 分）**：存在一致性差异或个别申报异常，需要投后经理在 5 个工作日内与企业 CFO 核对原因；
- **红（< 60 分）**：多项规则命中或申报逾期叠加，建议立即启动投后专项核查、要求书面说明与整改时间表。

---

## 五、健康分与规则阈值速查表

**① R1–R6 校验规则**

| 编号 | 名称 | 校验口径 | 命中阈值 | 严重度分档（按差异率） |
|---|---|---|---|---|
| R1 | 三表勾稽 | 净利润 ≈ 未分配利润变动 + 分红 | 差异率 > 3% | > 20% 高；> 5% 中；其余 低 |
| R2 | 账税一致性 | 账面营业收入 vs 增值税申报销售额 | 差异率 > 5% | > 15% 高；> 5% 中；其余 低 |
| R3 | 所得税匹配 | 利润总额 vs 企税申报应纳税所得额 | 差异率 > 10% | > 25% 高；> 10% 中；其余 低 |
| R4 | 发票-申报比对 | 开票金额 vs 增值税申报收入 | 差异率 > 5% | > 15% 高；> 5% 中；其余 低 |
| R5 | 现金流勾稽 | 收现比 = 销售收现 / 营业收入 | 收现比 < 0.7 或 > 1.3 | < 0.5 或 > 1.5 高；其余命中为 中 |
| R6 | 社保个税匹配 | 工资总额 vs 社保/个税申报基数 | 差异率 > 10% | > 25% 高；> 10% 中；其余 低 |

（差异率 = 两口径差额的绝对值 ÷ 前一科目金额的绝对值。）

**② 申报状态机**（相对数据基准日）

| 状态 | 判定 | 扣分 |
|---|---|---|
| 已申报 | 有申报日期记录 | 0 |
| 未到期 | 未申报，距截止日 > 3 天 | 0 |
| 临期 | 未申报，距截止日 ≤ 3 天 | 4 分 / 项 |
| 逾期 | 未申报，已过截止日 | 12 分 / 项 |

**③ 扣分模型与等级**

- 健康分 = 100 − Σ 规则扣分（严重度 高 30 / 中 20 / 低 10）− Σ 申报扣分（逾期 12 / 临期 4），下限为 0；
- 等级：**绿 ≥ 90；黄 60–89；红 < 60**；
- 滞纳金 = 税额 × 0.05% × 逾期天数（万分之五/日）。

---

## 六、常见问题 FAQ

**Q1：数据基准日为什么是固定的 2026-06-20，而不是今天？**
这是演示原型，`data/demo.js` 中的 `meta.baseDate` 写死为 2026-06-20，目的是让「临期」「逾期」「滞纳金」等状态在任何一天打开演示时都稳定可复现。接入真实数据后应改为动态当日，申报状态机会自动按当日重新计算。

**Q2：如何替换成自己企业的数据？**
用文本编辑器打开 `app/data/demo.js`，在 `companies` 数组中仿照现有企业修改或新增条目：保留 `finance`（三表关键科目）和 `filings`（申报记录）的字段名与结构不变，仅替换数值与企业元信息，保存后刷新浏览器即可。规则引擎 `js/rules.js` 无需改动。后续正式版本规划支持 API 接入与 Excel 模板导入（SheetJS）。

**Q3：双击 `index.html` 打不开或页面空白怎么办？**
1. 确认使用 Chrome 或 Edge 浏览器（不要用微信内置浏览器直接打开本地文件）；
2. 确认 `app` 文件夹结构完整（`index.html`、`data/demo.js`、`js/rules.js`、`js/app.js` 四个文件缺一不可）；
3. 若仍异常，改用「方式 B」：在 `app` 目录下执行 `python -m http.server 8080`，再访问 `http://localhost:8080`。

**Q4：页面上的「AI 风险解读」是真的 AI 吗？**
原型中不是。它由 `js/rules.js` 内置的模板（`AI_TEMPLATES`）把规则命中结果（企业名、差异金额、差异率）拼接进预设文案生成，属于模拟 AI 输出，用于验证产品交互形态。正式环境的方案是把规则命中结果作为 prompt 上下文调用大模型接口，生成真实的解读与建议。因此演示中所有解读均不构成税务或投资意见。

**Q5：点「打印 / 导出 PDF」没有反应？**
该按钮调用的是浏览器原生打印功能（`window.print()`），正常情况下会弹出打印对话框。若未弹出，请检查浏览器是否拦截了弹窗，或直接用快捷键 `Ctrl+P` 打开打印对话框；在对话框中选择「另存为 PDF」即可导出。

**Q6：规则阈值和扣分标准可以调整吗？**
可以。所有阈值、严重度分档、扣分值都集中在 `app/js/rules.js` 中：每条规则的 `threshold` 与 `severityByRate` 参数控制命中与分档，`SCORE_DEDUCT` 控制扣分，改完保存并刷新浏览器即生效。

**Q7：申报日历为什么只显示 2026 年 6 月？**
日历网格按演示数据的申报截止日所在月份（2026 年 6 月）渲染。截止日在 6 月之外的事项（如 2026-07-15 的企业所得税季报）会显示在企业详情页的「本期申报状态」表中，但不进入日历网格。

---

## 七、演示小贴士

**投屏与分辨率**

1. 建议使用 1920 × 1080 及以上分辨率的显示器或投影；
2. 浏览器窗口保持最大化（内容区最大宽度 1160px，宽屏居中显示）；
3. 如字号偏小，可用 `Ctrl` + `+` 放大到 110%–125%，布局不会错乱。

**建议讲解顺序（约 5–8 分钟）**

1. **组合总览**（1 分钟）：先讲 4 张 KPI 卡——「平均 67 分、2 家红色预警、7 项待申报」，建立组合视角；
2. **风险分布与排行**（1 分钟）：指出 R1–R6 各命中 1 家、6 项逾期集中在红色企业，点明「红黄绿」分层逻辑；
3. **点击红色企业卡片进入详情**（2–3 分钟，推荐砺芯半导体）：对照「三表数据区」讲 R1 差异（净利润 150 vs 未分配利润变动 200），展示「命中 + 严重度高」徽章与 AI 解读卡片，强调「规则定位问题、AI 解释问题、建议指导行动」；
4. **申报监测日历**（1 分钟）：切到日历页，讲红色小条（逾期）与琥珀色小条（临期）、提醒列表的滞纳金估算口径（万分之五/日）；
5. **体检报告页**（1 分钟）：从详情页点「生成体检报告 →」，展示五段式报告结构，现场 `Ctrl+P` 导出一次 PDF 收尾；
6. **收尾话术**（30 秒）：说明全部数据为虚构、AI 解读为模板模拟，真实版可接入企业填报数据与大模型接口。

---

*本原型仅为产品验证 Demo，全部企业、财务与申报数据均为虚构；AI 解读为模板化模拟输出，不构成任何税务或投资意见。*
